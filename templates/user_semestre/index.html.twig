{% extends 'base.html.twig' %}

{% block title %}UserSemestre index{% endblock %}

{% block body %}
    <h1>UserSemestre index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>AsAjac</th>
                <th>AsValide</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user_semestre in user_semestres %}
            <tr>
                <td>{{ user_semestre.id }}</td>
                <td>{{ user_semestre.asAjac ? 'Yes' : 'No' }}</td>
                <td>{{ user_semestre.asValide ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('user_semestre_show', {'id': user_semestre.id}) }}">show</a>
                    <a href="{{ path('user_semestre_edit', {'id': user_semestre.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('user_semestre_new') }}">Create new</a>
{% endblock %}
