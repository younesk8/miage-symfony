<form method="post" action="{{ path('description_diplome_delete', {'id': description_diplome.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ description_diplome.id) }}">
    <button class="btn">Delete</button>
</form>
