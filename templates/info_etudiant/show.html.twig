{% extends 'base.html.twig' %}

{% block title %}InfoEtudiant{% endblock %}

{% block body %}
    <h1>InfoEtudiant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ info_etudiant.id }}</td>
            </tr>
            <tr>
                <th>NumEtu</th>
                <td>{{ info_etudiant.numEtu }}</td>
            </tr>
            <tr>
                <th>RegimeSpecial</th>
                <td>{{ info_etudiant.regimeSpecial }}</td>
            </tr>
            <tr>
                <th>AdresseEtu</th>
                <td>{{ info_etudiant.adresseEtu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('info_etudiant_index') }}">back to list</a>

    <a href="{{ path('info_etudiant_edit', {'id': info_etudiant.id}) }}">edit</a>

    {{ include('info_etudiant/_delete_form.html.twig') }}
{% endblock %}
