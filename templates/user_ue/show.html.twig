{% extends 'base.html.twig' %}

{% block title %}UserUE{% endblock %}

{% block body %}
    <h1>UserUE</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_u_e.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_u_e_index') }}">back to list</a>

    <a href="{{ path('user_u_e_edit', {'id': user_u_e.id}) }}">edit</a>

    {{ include('user_u_e/_delete_form.html.twig') }}
{% endblock %}
